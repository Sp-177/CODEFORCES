
#include<iostream>
#include<bits/stdc++.h>

using namespace std;

#define ll long long

int main(){
ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ll t=1;
    // cin>>t;
    while(t--){
        string a,b;cin>>a>>b;
        ll n=a.length(),m=b.length();
        vector<int>nums;
        int num_b=0;
        
    }

    return 0;
}