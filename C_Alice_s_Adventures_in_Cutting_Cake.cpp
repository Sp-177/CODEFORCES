#include<bits/stdc++.h>
using namespace std;

long long ans=0LL;
 bool sol(long long *arr,long long n,long long m,long long k,long long k1,int i,int s){
    if(i==n&&m<0)return true;
    

 }
int main(){
    ios_base::sync_with_stdio(false);cin.tie(nullptr);
    long long n,m,k;cin>>n>>m>>k;
    long long arr[n];
    for(int i=0;i<n;i++)cin>>arr[i];

}